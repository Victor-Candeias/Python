from controllers.device_manager import DeviceManager
from controllers.configuration_manager import ConfigurationManager

device_manager = DeviceManager()
configuration_manager = ConfigurationManager()
